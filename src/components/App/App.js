import React from 'react';
import styles from './App.css';

const arr = [1, 2, 3, 4, 5];
console.log({...arr});

const App = () => (
  <div className={styles.wrapper}>
    Hello World!!!
  </div>
);

export default App;
